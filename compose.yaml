services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=elearning_kma'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
